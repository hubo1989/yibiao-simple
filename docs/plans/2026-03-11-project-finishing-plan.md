# 2026-03-11 项目收口与拆分提交执行计划

## 目标

将当前工作区内已存在但尚未收口的大块改动，整理为可验证、可回滚、可追踪的原子提交，并持续推进到以下主线全部完成：

1. 标书增强 UI
2. 知识库 / RAG
3. 多模型配置
4. 请求日志
5. 企业化前端壳子

同时要求：
- 每条主线尽量独立提交
- 不混入本地噪音文件
- 每个提交都能说明范围与目的

---

## 当前已确认状态

### 已存在提交
- `615820a` feat: persist outline scoring bindings on chapters
- `874765d` feat: add bid-focused project generation endpoints
- `656f33b` feat: expose bid enhancement APIs in frontend
- `dbec2e1` chore: ignore local agent and generated files

### 当前未收口改动（按主线归类）

#### A. 标书增强 UI
目标：把已存在的增强能力真正接到前端页面入口。

重点内容：
- 评分项响应清单入口
- 评分点反向补强章节入口
- 技术参数 / 条款逐条响应入口
- 入口位置、按钮、交互、结果展示闭环

计划动作：
1. 识别现有 API 封装位置与调用点
2. 在页面中接入实际按钮和弹窗/面板
3. 验证不会破坏现有目录/章节流
4. 单独提交

完成标志：
- 用户可以在前端直接触发上述能力
- 不只是 API 存在，而是 UI 可用

---

#### B. 知识库 / RAG
目标：把知识库文档管理与生成注入链路收口。

重点内容：
- 知识库文档上传 / 列表 / 删除
- 文档类型（历史标书、企业资料等）
- 检索服务、embedding、索引逻辑
- 在章节生成时注入知识库上下文

计划动作：
1. 梳理 model / schema / router / service / migration
2. 确认生成链路里 knowledge_context 注入是否完整
3. 补足遗漏测试和必要文档
4. 单独提交

完成标志：
- 知识库链路完整
- 生成时可引用知识库素材

---

#### C. 多模型配置
目标：完成 provider 配置、多模型选择与默认模型逻辑。

重点内容：
- provider config 模型列表支持
- 默认模型与当前模型选择逻辑
- 前端配置页与缓存逻辑
- 相关接口和测试

计划动作：
1. 梳理 backend config / schema / tests
2. 梳理 frontend ConfigPanel / modelCache / setupTests
3. 验证模型选择行为一致性
4. 单独提交

完成标志：
- 模型配置前后端一致
- 默认模型与切换逻辑可用

---

#### D. 请求日志
目标：完成 request logging 的后端链路与前端查看入口。

重点内容：
- request_log model
- middleware
- router / schema
- 管理页面或查看页面
- migration

计划动作：
1. 梳理 logging 中间件与模型
2. 验证 API 和页面连通
3. 补足测试
4. 单独提交

完成标志：
- 请求日志可落库、可查看

---

#### E. 企业化前端壳子
目标：整理企业化页面壳层、导航和基础布局。

重点内容：
- AuthShell
- layout / header / routes
- ProjectManager
- ProjectSettings
- BidReview
- KnowledgeBase
- RequestLogs

计划动作：
1. 将纯页面结构与业务逻辑分离
2. 优先把“壳子类改动”独立提交
3. 避免与 RAG / 日志 / 模型逻辑混提

完成标志：
- 页面结构清晰
- 路由与导航稳定

---

## 执行顺序

按“最容易形成原子提交”优先：

1. 标书增强 UI
2. 知识库 / RAG
3. 多模型配置
4. 请求日志
5. 企业化前端壳子

说明：
- 若某主线内部仍混叠，则继续拆为 2 个更小提交
- 如果某块验证成本过高，先跳过，做更容易落 hash 的块

---

## 提交规则

每个提交必须满足：
- 范围单一
- message 直接说明功能点
- 不夹带无关文件

每次提交后同步：
- commit hash
- 改动范围
- 剩余项
- 下一步

---

## 风险与处理

### 风险 1：工作区混叠严重
处理：
- 先按文件归类
- 使用选择性暂存
- 必要时拆成多个连续提交

### 风险 2：前后端逻辑交叉
处理：
- 优先提交闭环最完整的一块
- 不强求一次性收完所有相关文件

### 风险 3：汇报失真
处理：
- 后续只汇报真实新 hash 或明确阻塞
- 不再用泛化进度代替产出

---

## 立即执行项

从现在开始，立刻执行：

### 第一优先级
- 收口“标书增强 UI”并形成新提交

### 第二优先级
- 收口“知识库 / RAG”并形成新提交

本计划创建后立即生效。
